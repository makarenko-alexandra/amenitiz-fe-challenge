import { LoadingContainer } from './GrandmasterProfile.styles';

const LoadingFallback = () => {
  return (
    <LoadingContainer>
      <h2>Loading grandmaster profile...</h2>
    </LoadingContainer>
  );
};

export default LoadingFallback; 